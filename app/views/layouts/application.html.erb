<!DOCTYPE html>
<html>
  <head>
    <title>Lista zadań</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="bg-gradient-to-r from-blue-600 to-indigo-600 text-white">
      <div class="container-narrow py-4 flex items-center justify-between">
        <%= link_to "Lista zadań", root_path, class: "text-lg font-semibold" %>
        <%= link_to "Nowe zadanie", new_task_path, class: "btn btn-secondary bg-white/10 border-white/20 hover:bg-white/20 text-white" %>
      </div>
    </header>

    <main class="py-6">
      <div class="container-narrow">
        <% if notice %><div class="alert alert-success mb-4"><%= notice %></div><% end %>
        <% if alert %><div class="alert alert-error mb-4"><%= alert %></div><% end %>
      </div>
      <%= yield %>
    </main>
  </body>
</html>
