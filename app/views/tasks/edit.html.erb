<div class="container-narrow">
  <h1 class="text-2xl font-bold mb-4"><%= action_name == "new" ? "Nowe zadanie" : "Edytuj zadanie" %></h1>
  <%= render "form", task: @task %>
  <div class="mt-4">
    <%= link_to "Powrót", tasks_path, class: "underline text-sm" %>
  </div>
</div>
